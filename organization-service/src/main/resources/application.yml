# Spring Boot Application Configuration

spring:
  # Database Configuration (currently commented out)
#   datasource:
#     url: jdbc:mysql://localhost:3306/organization_db  # MySQL database connection URL
#     username: root                                    # Database username
#     password: root                                    # Database password

#   jpa:
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.MySQL8Dialect  # Specifies the MySQL 8 dialect for Hibernate
#     hibernate:
#       ddl-auto: update                               # Automatically updates database schema

  # Application Name Configuration
  application:
    name: organization-service                          # Service name used for service discovery

  # Spring Cloud Config Server Configuration
  config:
    import: optional:configserver:http://localhost:8888 # URL for the Config Server (optional connection)

  # RabbitMQ Message Broker Configuration
  rabbitmq:
    host: localhost                                     # RabbitMQ server host
    port: 5672                                         # Default RabbitMQ port
    username: guest                                    # RabbitMQ username
    password: guest                                    # RabbitMQ password

# Server Configuration (currently commented out)
# server:
#   port: 8083                                         # Application server port

# Eureka Service Discovery Configuration (currently commented out)
# eureka:
#   client:
#     serviceUrl:
#       defaultZone: http://localhost:8761/eureka/     # Eureka server URL for service registration
#   instance:
#     prefer-ip-address: true                          # Use IP address instead of hostname
#     ip-address: 172.23.16.1                         # Explicitly set IP address
#     instance-id: ${spring.application.name}:${server.port}  # Custom instance ID format

